# Grid and responsive layout

Use `Responsive.Breakpoint.*` and `Responsive.Layout.*`. Breakpoints represent behavior, not devices. Reflow before hiding; preserve primary actions and critical state. Tables use column priority, containment, or a documented alternate layout. Test long names, destinations, currencies, translations, and empty values. Prefer container-aware components to one-off breakpoints.


# Agent instructions

For ParyatechOS design/frontend work, load `SKILL.md`. Treat `Tokens/`, `CSS/`, and `tokens/*.json` as generated. When a value or pattern is absent, propose it explicitly instead of inventing it.


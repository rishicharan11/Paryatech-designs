# Spacing

Use `Spacing.Component.*` when available, then `Spacing.Scale.*`; never `Core.Spacing.*`. Tighter spacing binds related items, regular spacing organizes a component, and larger spacing separates regions. Prefer parent `gap` and shared padding over child margins. Never add arbitrary values for one screenshot. Preserve hierarchy across density modes.

